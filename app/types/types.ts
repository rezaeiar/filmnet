export type ShowcaseSlideType = {
    id: number,
    title: string,
    image: string,
    label: string,
    isComming?: boolean,
    isFree?: boolean,
    inNew: boolean,
    genre?: string,
    time?: string
    Production: string,
    isSerie?: boolean
}
export type CollectionSlideType = {
    id: number,
    title: string,
    image: string
}